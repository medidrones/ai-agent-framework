[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "atlas-agent-config"
version = "0.1.0"
description = "Configuração declarativa e composition root do Atlas"
requires-python = ">=3.12"
license = "MIT"
authors = [{ name = "Jorge Medina" }]
classifiers = [
    "Development Status :: 2 - Pre-Alpha",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Typing :: Typed",
]
dependencies = [
    "atlas-agent-adapters>=0.1.0",
    "atlas-agent-core>=0.1.0",
    "pydantic>=2.10,<3",
    "pyyaml>=6.0,<7",
]

[project.urls]
Repository = "https://github.com/Medicode/ai-agent-framework"

[tool.hatch.build.targets.wheel]
packages = ["src/atlas_agents"]

[tool.hatch.build.targets.sdist]
include = ["src/atlas_agents/config", "tests", "pyproject.toml"]
