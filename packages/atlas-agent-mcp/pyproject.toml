[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "atlas-agent-mcp"
version = "0.1.0"
description = "Integração opcional do Atlas Agent Framework com MCP"
requires-python = ">=3.12"
license = "MIT"
authors = [{ name = "Jorge Medina" }]
classifiers = [
    "Development Status :: 2 - Pre-Alpha",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Typing :: Typed",
]
dependencies = [
    "atlas-agent-core>=0.1.0",
    "mcp>=2.2.0,<3",
    "pydantic>=2.10",
]

[project.urls]
Repository = "https://github.com/Medicode/ai-agent-framework"

[tool.hatch.build]
dev-mode-dirs = ["src"]

[tool.hatch.build.targets.wheel]
packages = ["src/atlas_agents"]

[tool.hatch.build.targets.sdist]
include = ["src/atlas_agents/mcp", "tests", "pyproject.toml"]
