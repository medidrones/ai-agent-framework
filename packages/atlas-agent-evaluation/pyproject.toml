[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "atlas-agent-evaluation"
version = "0.1.0"
description = "Avaliação provider-neutral para o Atlas Agent Framework"
requires-python = ">=3.12"
license = "MIT"
authors = [{ name = "Jorge Medina" }]
classifiers = [
    "Development Status :: 2 - Pre-Alpha",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Typing :: Typed",
]
dependencies = [
    "atlas-agent-core>=0.1.0",
    "pydantic>=2.10",
]

[project.urls]
Repository = "https://github.com/Medicode/ai-agent-framework"

[tool.hatch.build]
dev-mode-dirs = ["src"]

[tool.hatch.build.targets.wheel]
packages = ["src/atlas_agents"]

[tool.hatch.build.targets.sdist]
include = ["src/atlas_agents/evaluation", "tests", "pyproject.toml"]
